# Lloyds SME lead-scoring — Python dependencies
# Derived from the imports actually used across notebooks 1-5 and paths.py.
# Versions are those this build was developed and tested against.
#
# Python >= 3.12. xgboost >= 3.4.1 and shap >= 0.52 have dropped 3.11; everything else
# still supports it. Developed on 3.13. See README for the .venv setup.

# --- core ---
pandas>=3.0.1
numpy>=2.4.2
requests>=2.34.2
python-dotenv>=1.1.0          # reads the Companies House API keys from .env
tqdm>=4.67.1                  # progress bars on the long API pulls

# --- modelling ---
# PINNED, not >=: Model/model.joblib is a pickle written by 1.8.0. A newer sklearn
# unpickles it with six InconsistentVersionWarnings warning that results "might be
# invalid". Output was verified byte-identical under 1.9.0, but that is not guaranteed
# to hold, and the manifest records sklearn_version so the mismatch is detectable.
scikit-learn==1.8.0           # LogisticRegression (primary), pipeline, grid search
joblib>=1.5.3                 # persists the fitted pipeline to Model/model.joblib
xgboost>=3.4.1                # comparator in 4_model.ipynb
shap>=0.52.0                  # feature attribution in 4_model.ipynb
matplotlib>=3.10.8

# --- GDELT / BigQuery (needed only for 2_GDELT.ipynb Parts 3-4) ---
# Part 5 (postcode -> region) uses postcodes.io and needs none of these.
google-cloud-bigquery>=3.42.1
db-dtypes>=1.7.0              # BigQuery -> pandas type mapping
pyarrow>=23.0.1               # Parquet caches under API/GDELT/BigQuery Cache files/

# --- gen-AI layer (LLM agent / RAG / eval on top of the scored leads) ---
anthropic>=1.5.0              # Claude API (SDK 1.x)
langchain>=1.4.0
langchain-anthropic>=1.7.2    # lets LangChain / LangGraph / ragas call Claude
langgraph>=1.2.11
langsmith>=0.12.4             # tracing; swap for langfuse if preferred
mcp>=1.30                     # pinned <2 by langchain-mcp-adapters; server.py supports both APIs
ragas>=0.4.3                  # RAG evaluation
# PINNED BELOW 0.4: ragas 0.4.3 still imports langchain_community.chat_models.vertexai,
# which langchain-community 0.4 removed. 0.3.x works with langchain-core 1.x. Lift
# the cap once ragas stops importing it.
langchain-community<0.4
ipykernel>=7.3.0              # so .venv shows up as a Jupyter kernel
grandalf>=0.8                 # graph.get_graph().draw_ascii() in the production notebooks
